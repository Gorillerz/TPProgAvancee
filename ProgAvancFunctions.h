#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void UserInterface(int* , int );

void Affiche(int* , int , void (*f) (int*, int));

void Contenu(int *, int );
void PairsImpairs(int *, int );
void Pincer(int *, int );
